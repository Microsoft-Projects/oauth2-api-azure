{
  "name": "oauth2-usersignin",
  "version": "1.0.0",
  "description": "Sample API with OAuth2 User Authentication strategy",
  "main": "index.js",
  "scripts": {
    "start": "npm run build && ts-node ./src/index",
    "build": "npm run clean && tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -r dist && mkdir dist"
  },
  "author": "Danny Garber <dannyg@microsoft.com> (https://www.linkedin.com/in/danny-garber-19b535/)",
  "license": "MIT",
  "dependencies": {
    "ts-node": "^8.0.2"
  },
  "devDependencies": {
    "dotenv": "^6.2.0",
    "envalid": "^4.1.4",
    "express": "^4.16.4",
    "express-session": "^1.15.6",
    "oauth2-api-azure": "^0.0.4",
    "typescript": "^3.2.4"
  }
}
